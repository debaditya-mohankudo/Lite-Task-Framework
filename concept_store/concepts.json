{
  "concepts": {
    "advisory-nudge-dispatcher": {
      "confidence": 0.8,
      "contracts": [
        "introspection_nudge(report, task_id, conn) returns None or a string; never raises, never mutates the report or the task",
        "Fires only when the report has an unpromoted lesson AND the task has never cited any memory",
        "finish_nudge(task) returns None or a string; fires only when task.introspection is empty",
        "drift_reflection_nudge(active_task_id, active_task_title='', active_task_phase='', active_task_next_item='', call_count=None, active_task_completed_items=None) returns None or a string; fires every _DRIFT_NUDGE_INTERVAL calls (the constant's current value, not repeated here, is the source of truth) when call_count is supplied, else every call. No `scope` param — the caller (taskfw/drift_hook.py) resolves scope itself via TaskStore before calling. active_task_next_item (next_open_item(task)) and active_task_completed_items (completed_items(task), added task:1d23eb67) are both derived fresh from task.resolution by the caller each time, never stored. Label shape: 'task:<id> (<title>) [<phase>] — done: <item>; <item> — next: \"<item>\"', each segment appended only when its input is non-empty.",
        "completed_items(task) -> list[str] returns every checked resolution item's text in checklist order, derived from task.resolution on every call (task:1d23eb67) — same non-stored-derivation pattern as next_open_item and is_implemented.",
        "phase_label(phase: dict[str,bool]) -> str collapses task_phase's three independent booleans into a single word — 'grooming' | 'implementation' | 'introspection' | 'done' — by returning the first stage not yet reached in that order. Lives next to task_phase, takes task_phase's dict output rather than a Task, so a caller composes the two rather than task_phase growing a mode flag (task:afd462e3).",
        "is_implemented(task) -> bool is the pure predicate (task.progress total>0 and done==total), status-independent — extracted from finish_reminder_nudge's inline check (task:bf95ced8)",
        "finish_reminder_nudge(task) returns None or a string; fires when is_implemented(task) is True AND task.status == 'open'",
        "task_phase(task) -> {groomed, implemented, introspected} (all bool), fully derived every call, never stored — exposed via mcp_server.tasks__phase (task:bf95ced8)",
        "stale_memory_nudge(memory: dict) -> str | None; fires when memory['standing'] is 'disputed' or 'contradicted', wired onto task_memory__link via mcp_server._stale_memory_hook. Stateless like finish_reminder_nudge, not throttled like drift_reflection_nudge — re-fires on every link() call that still finds the memory disputed/contradicted, including an idempotent re-link, since task_memory__link is an infrequent call with no need for call-count throttling (task:582dba10)",
        "tool_called(name, pre=None, post=None): the name-carrying wrapper around one MCP tool call. On a clean exit it logs 'tool=<name> OK' when call.result has no 'error' key (or is not a dict, e.g. tasks__list's bare list), 'tool=<name> REFUSE rule=<rule>' when it does; on an exception it logs 'tool=<name> ERROR <type>: <msg>' and re-raises. post fires on any non-refused, dict-shaped result.",
        "combine(*hooks) -> Callable[[dict], None] is a composite hook: runs each hook in order, same Callable[[dict], None] shape as any single one, so a tool needing several nudges (e.g. tasks__update wants both drift_reflection_nudge and finish_reminder_nudge) composes them at its own decorator line instead of tool_called/_tool needing a special multi-hook parameter.",
        "apply_nudge(result, key, nudge) is the one shared result-mutation helper for every nudge function — call the nudge function yourself, then apply_nudge(result, key, nudge) sets result[key] only if nudge is truthy.",
        "ungroomed_progress_nudge(task) returns None or a string; fires when task.progress has at least one checked item AND task.grooming is empty — matches task_phase()'s own bool(task.grooming) definition of 'groomed' so the two can never disagree. Stateless like finish_reminder_nudge and stale_memory_nudge: re-checks current state on every qualifying save rather than firing once and remembering it fired (task:a9e7082d)."
      ],
      "created_at": "2026-08-01T10:02:54.174596+00:00",
      "description": "Single home for advisory, non-blocking response annotations (nudges) AND unconditional per-call logging on MCP tool results. Distinct from lifecycle.py (blocking rule enforcement, checked before a write) and store.py (persistence) — a nudge never refuses a write and never changes what's stored, it only surfaces something true that would otherwise be silent; logging never changes a result either, it only records that a call happened. Exists to prevent mcp_server.py tools from each growing their own private _foo_check() helper. tool_called is the pre/post-hook shape nudges run through, self-contained inside taskfw's own server, scoped only to taskfw's own tools. Every one of mcp_server.py's ~35 registered tools goes through it via exactly one mechanism: the _tool decorator in mcp_server.py.",
      "evidence": [
        "taskfw/dispatcher.py:tool_called",
        "taskfw/dispatcher.py:combine",
        "taskfw/dispatcher.py:introspection_nudge",
        "taskfw/dispatcher.py:finish_nudge",
        "taskfw/dispatcher.py:drift_reflection_nudge",
        "taskfw/dispatcher.py:completed_items",
        "taskfw/dispatcher.py:next_open_item",
        "taskfw/dispatcher.py:phase_label",
        "taskfw/dispatcher.py:is_implemented",
        "taskfw/dispatcher.py:finish_reminder_nudge",
        "taskfw/dispatcher.py:stale_memory_nudge",
        "taskfw/dispatcher.py:task_phase",
        "taskfw/dispatcher.py:apply_nudge",
        "taskfw/dispatcher.py:ungroomed_progress_nudge",
        "taskfw/mcp_server.py:_tool, _drift_reflection_hook, _finish_reminder_hook, _finish_hook, _introspection_hook, _stale_memory_hook, _ungroomed_progress_hook",
        "tests/test_dispatcher.py::TestToolCalled",
        "tests/test_dispatcher.py::TestCombine",
        "tests/test_dispatcher.py::TestStaleMemoryNudge",
        "tests/test_dispatcher.py::TestUngroomedProgressNudge",
        "tests/test_dispatcher.py::TestDriftReflectionNudge",
        "tests/test_dispatcher.py::TestCompletedItems",
        "tests/test_dispatcher.py::TestPhaseLabel",
        "tests/test_mcp_tools.py::TestToolCallLogging",
        "tests/test_mcp_tools.py::TestStaleMemoryNudge",
        "tests/test_mcp_tools.py:TestUpdate",
        "tests/test_mcp_tools.py:TestChecklist"
      ],
      "invariants": [
        "Never blocks or changes a write — advisory only",
        "No name-keyed dispatch table (no GATES-style registry) — explicitly reconsidered and re-rejected during task:58782207's design pass: a registry would let a rename silently desync a string key from the tool it names, adds a second place to read to know what a tool does on success, and accretes per-tool conditions into the table itself. Every hook stays local and explicit at its own tool's decorator line, composed via combine() when more than one applies.",
        "Every one of mcp_server.py's ~35 registered tools produces exactly one tool_called log line per call — verified by tests/test_mcp_tools.py::TestToolCallLogging::test_every_registered_tool_logs_exactly_once_when_called, which enumerates the live m.mcp.list_tools() registry rather than a hand-maintained list",
        "Log level is INFO, one terse line per call — tool name and outcome only, never arguments, to avoid flooding `logs` with payloads or PII",
        "task_phase's 'groomed' has no separate groomed_at flag — a task is groomed exactly when task.grooming is non-empty",
        "finish_reminder_nudge closes a distinct gap from finish_nudge: finish_nudge fires AFTER tasks__finish when introspection is missing; finish_reminder_nudge fires on tasks__check_item/tasks__update BEFORE finish, when the checklist is complete but finish was never called",
        "stale_memory_nudge never calls supersede()/forget() itself — advisory only, same as every other nudge here, consistent with task-introspection's 'never unilaterally delete stale memories' rule",
        "ungroomed_progress_nudge is the first nudge built as a thin wrapper deriving its condition the same way task_phase() derives 'groomed' (bool(task.grooming)), rather than re-deriving its own independent condition from raw task fields — still fits the plain Callable[[dict], None] shape with no new mechanism.",
        "phase_label never throws on a real Task's task_phase() output: grooming/introspection are dataclass fields with default_factory (never None) and progress is a derived property, so the dict it reads is always well-formed (task:afd462e3)."
      ],
      "last_validated": "2026-08-19T07:00:00.000000+00:00",
      "module": "taskfw/dispatcher.py",
      "name": "advisory-nudge-dispatcher"
    },
    "commit-backfill-recovery": {
      "confidence": 0.92,
      "contracts": [
        "backfill(repo, since=None, dry_run=False) -> {scanned, linked, already_linked, unknown_tasks, dry_run}",
        "git_log(repo, since) -> [(sha, message)], empty when the path is not a git repo",
        "extract_task_ids(text) -> every task:<id> reference, de-duplicated, order preserved"
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "Rebuilds commit-to-task links by scanning git history for task:<id> references. There is no automatic capture: a commit is linked by an explicit tasks__add_commit call, made by the commit skill after a successful commit, and nothing enforces that call happens. This exists because that gap is invisible by construction: a task with no commits looks identical to one whose commits were never linked.",
      "evidence": [
        "taskfw/backfill.py:backfill",
        "tests/test_backfill.py:TestBackfill"
      ],
      "invariants": [
        "Idempotent — add_commit is a no-op for an already-recorded pair, so repeated runs are safe",
        "A non-git directory is not an error, it is zero commits scanned",
        "Git history is the durable record; the commit map is a cache over it, and this rebuilds the cache from scratch",
        "Never depends on tasks__add_commit having been called — re-derives every link straight from git log"
      ],
      "last_validated": "2026-08-01T06:05:35.208983+00:00",
      "module": "taskfw/backfill.py",
      "name": "commit-backfill-recovery"
    },
    "concept-store-name-keyed-map": {
      "confidence": 0.93,
      "contracts": [
        "ConceptStore(repo) loads on construction; missing file is empty, not an error",
        "list(module=\"\") / get(name) / modules() / search(query) / uncovered(modules) are reads",
        "upsert(concept) MERGES and returns the merged concept; delete(name) -> bool",
        "concept__* MCP tools take repo explicitly, with no default",
        "concept__get returns {found: bool, concept: {...}} on both branches — never the concept's fields flattened onto the response"
      ],
      "created_at": "2026-08-01T01:03:15.785905+00:00",
      "description": "JSON-backed store of architectural concepts for a repo, plus the concept__* MCP tools over it. Makes the framework self-contained: before this, task-framework had a concept store it could not read without another project running its server. The on-disk format is deliberately identical to the established one so a store stays readable by either implementation and neither project owns the format. As of task:756c14db, this is the SOLE concept__* MCP surface for JSON-format concept stores — claude-hooks' own duplicate (src/tools/concept.py) was removed once concept__get's response shape was corrected here to match what claude-hooks' skills already depended on by bare tool name.",
      "evidence": [
        "taskfw/concepts.py:ConceptStore.load",
        "taskfw/concepts.py:ConceptStore.save",
        "taskfw/mcp_server.py:concept__get",
        "tests/test_concept_tools.py:TestShapeHandling",
        "tests/test_concept_tools.py:TestTools"
      ],
      "invariants": [
        "concepts is a NAME-KEYED MAP on disk, never a list — a list is rejected at load with a message naming the problem rather than failing later with AttributeError",
        "Every accessor iterates self._concepts, never the loaded file: calling .values()/.items() on the top level yields \"concepts\" and \"meta\" as if they were concepts",
        "A store without the {concepts, meta} wrapper is treated as the bare map, detected by absence of the key rather than by inspecting contents",
        "upsert merges rather than overwrites, so updating one field cannot silently drop invariants the caller did not mention; an explicit empty list still clears",
        "created_at survives an update; last_validated is restamped on every upsert",
        "repo is always an explicit argument — a store is never tied to whichever server is running",
        "save() writes with ensure_ascii=False — without it, every em-dash and non-ASCII character in an untouched concept gets re-escaped on any single-entry write, producing a whole-file diff for a one-entry change",
        "concept__get's found:True branch nests under concept: {...} rather than flattening fields onto the response, so \"found\" cannot collide with a real field name and a caller does not need to know which implementation answered the call"
      ],
      "last_validated": "2026-08-01T06:15:42.002982+00:00",
      "module": "taskfw/concepts.py",
      "name": "concept-store-name-keyed-map"
    },
    "connection-pragmas-single-point": {
      "confidence": 0.93,
      "contracts": [
        "connect(path=None) -> sqlite3.Connection with row_factory=Row, migrated on first use per path",
        "transaction(conn) commits on success and rolls back on any exception"
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "connect() is the only place connection pragmas are set, and runs migrations once per database path. WAL, busy_timeout, and foreign_keys are applied here rather than left to callers because several processes write the same file concurrently.",
      "evidence": [
        "taskfw/db/connect.py:connect",
        "tests/test_foundation.py:TestConnectionPragmas"
      ],
      "invariants": [
        "WAL, busy_timeout, and foreign_keys are set for every connection, never by a caller",
        "WAL is skipped for :memory:, where it does not apply",
        "Migration runs at most once per path per process"
      ],
      "last_validated": "2026-08-01T00:00:00+00:00",
      "module": "taskfw/db/connect.py",
      "name": "connection-pragmas-single-point"
    },
    "drift-hook-posttooluse-entrypoint": {
      "confidence": 0.8,
      "contracts": [
        "build_nudge(store, scope, call_count) returns None if no active task for scope, else dispatcher.drift_reflection_nudge(...)'s result, passing call_count through unchanged and also passing next_open_item(active_task) and completed_items(active_task) (task:1d23eb67) computed fresh from the active task's resolution on every call",
        "main() reads JSON from stdin; a malformed/empty payload is treated as {} rather than raising",
        "main() reads payload.get('_taskfw_drift_call_count'); a missing or non-int value is normalized to None, which makes drift_reflection_nudge fire on every call (pre-task:1c8f0815 behavior)",
        "main() fails open on any exception from build_nudge (log.exception, return 0) — a crashing hook must never block the tool call it's attached to",
        "main() emits nothing on stdout (not even an empty JSON object) when there's no nudge — PostToolUse reads silence as 'nothing to add'",
        "_scope() mirrors mcp_server._scope(): TASKFW_SCOPE env var if set, else os.getcwd()"
      ],
      "created_at": "2026-08-15T19:44:25.908079+00:00",
      "description": "A standalone PostToolUse hook Claude Code (via claude-hooks' dispatcher.py) invokes as a fresh subprocess on every tool call in a session — not just taskfw's own MCP tools. Reads a JSON hook payload from stdin, looks up the active task for the calling scope via TaskStore, and if one exists, calls dispatcher.drift_reflection_nudge to build an awareness-nudge string, writing it back as PostToolUse hookSpecificOutput.additionalContext on stdout. Genuinely stateless: since it's a fresh subprocess per call, it cannot hold a cross-call counter itself (task:8be768df); claude-hooks' own dispatcher.py counts PostToolUse calls and passes the raw count in via stdin as \"_taskfw_drift_call_count\", forwarded unchanged. This module never decides whether to nudge or what to include — it only wires the stdin payload, the active-task lookup (including next_open_item and completed_items), and the stdout hook-output shape together.",
      "evidence": [
        "taskfw/drift_hook.py:build_nudge",
        "taskfw/drift_hook.py:main",
        "taskfw/drift_hook.py:_scope",
        "tests/test_drift_hook.py"
      ],
      "invariants": [
        "No relay through claude-hooks and no new daemon — registers as one more independent PostToolUse hook entry alongside claude-hooks' own client.py and concept_store/diff_hook.py",
        "task-framework remains sole owner of the active-task/drift decision even though claude-hooks supplies the raw call count — this module and dispatcher.drift_reflection_nudge are the only place that decides whether a given call fires"
      ],
      "last_validated": "2026-08-19T06:20:30.223951+00:00",
      "module": "taskfw/drift_hook.py",
      "name": "drift-hook-posttooluse-entrypoint"
    },
    "grooming-accuracy-aggregate": {
      "confidence": 0.92,
      "contracts": [
        "grooming_accuracy(store, limit) -> tallies, recurring risks, skipped introspections, self-report disagreements, and signals",
        "Scoped to status='done' only",
        "Everything except `signals` is a raw count or a list of task ids",
        "Exposed as tasks__grooming_accuracy"
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "Aggregates grooming risk grades across recent finished tasks — the only read that spans tasks rather than descending into one. Closes the feedback edge at the level of the loop itself: without it, 'repeated wrong grades mean grooming asks the wrong questions' is advice nothing can act on, because grades live inside each task and a pattern across tasks was writable but unreadable.",
      "evidence": [
        "taskfw/accuracy.py:grooming_accuracy",
        "taskfw/accuracy.py:_signals",
        "taskfw/mcp_server.py:tasks__grooming_accuracy",
        "tests/test_accuracy.py:TestDerivedNotReported",
        "tests/test_accuracy.py:TestSkippedIntrospection"
      ],
      "invariants": [
        "Tallies are DERIVED from per-risk `graded` values and never read from an introspection report's self-reported grooming_accuracy count — the same reason progress is derived from the checklist",
        "A disagreement between the self-reported tally and the derived one is reported, never silently resolved",
        "risks.total counts every risk seen, independently of the grade buckets, so an unrecognised grade cannot make a risk vanish from the total",
        "An unrecognised grade is bucketed and reported rather than dropped",
        "Only a FINISHED task with ungraded risks is a skipped introspection; an open one is work in progress",
        "predictive_value excludes ungraded risks from its denominator, so skipping the grading never flatters the score",
        "Ratio signals require MIN_SAMPLE graded risks; the skipped-introspection signal requires no sample, because one is already the loop not running",
        "A risk recurs only across DISTINCT tasks — a repeat within one task is a duplicate, not a pattern"
      ],
      "last_validated": "2026-08-01T00:00:00+00:00",
      "module": "taskfw/accuracy.py",
      "name": "grooming-accuracy-aggregate"
    },
    "lifecycle-single-rule-implementation": {
      "confidence": 0.96,
      "contracts": [
        "Every check returns Decision(allowed, rule, reason); Decision is falsy when denied",
        "check_save(task, previous=None, parent=None) is the single entry point for validating a mutation",
        "check_link_rel(rel) -> Decision gates tasks__link only, denying any rel not in TASK_EDGE_RELATIONS — mirrors check_status(status)'s exact closed-set-against-a-tuple shape. Deliberately NOT called from tasks__unlink: removal must always work for a legacy or out-of-vocabulary rel, or a bad historical row becomes permanently stuck (task:1c9d37d7).",
        "There is no check_active_switch anymore (removed task:f302eb2b) — tasks__set_active/push_active is non-destructive by construction, so nothing needs confirm=True to avoid losing state"
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "The single implementation of every task rule, imported by both the MCP tools and the hooks. Pure functions over (inputs, store) with no session, prompt, or host state. Deliberately small: body-template validation became schema validation, the hierarchy matrix collapsed to one sentence with two issue types, and commit traceability left entirely once the pre-commit gate was dropped. check_active_switch was deleted outright in task:f302eb2b once the active-task pointer became a non-destructive LIFO stack (push_active) instead of a single overwritten value — there was nothing left to guard against losing.",
      "evidence": [
        "taskfw/lifecycle.py:check_transition",
        "taskfw/lifecycle.py:check_save",
        "taskfw/lifecycle.py:check_link_rel",
        "taskfw/mcp_server.py:tasks__finish",
        "taskfw/mcp_server.py:tasks__link",
        "tests/test_lifecycle.py",
        "tests/test_mcp_tools.py:TestGraph::test_link_rejects_an_unrecognised_relation"
      ],
      "invariants": [
        "Rules are pure — the tests need no fixtures at all, which is the observable signal that portability holds",
        "Same-status is checked BEFORE the terminal rule, so re-saving a done or abandoned task is never an error; this is what makes tasks__finish idempotent",
        "done and abandoned are terminal; any non-terminal state may be abandoned",
        "The entire hierarchy rule is: an epic has no parent, and a task cannot be its own parent",
        "Denials log at INFO with rule, reason, and ids; allows log at DEBUG",
        "check_link_rel is the FIRST policy check ever added to tasks__link — before task:1c9d37d7 it had zero lifecycle validation, calling store().link() directly after only confirming both task ids exist",
        "tasks__finish/tasks__check_item's auto-finish share one _finish_task() helper (mcp_server.py) that pops the active-task stack only when the finished task_id is on top — a buried finish still marks the task done, it just skips the pop (task:f302eb2b)"
      ],
      "last_validated": "2026-08-15T17:19:39.616655+00:00",
      "module": "taskfw/lifecycle.py",
      "name": "lifecycle-single-rule-implementation"
    },
    "loop-memory-derived-standing": {
      "confidence": 0.9,
      "contracts": [
        "record(slug, text, task_id, kind) requires a real task; re-recording a slug updates the text and adds the new source",
        "recall(query, kind, limit, include_superseded, count_hit) excludes superseded memories by default",
        "link(slug, task_id, relation) records confirmed_by / contradicted_by / learned_from",
        "supersede(slug, by) keeps the row; forget(slug) deletes it",
        "Rejected is raised for shape errors only; the store enforces no policy",
        "Exposed as task_memory__record / recall / get / link / supersede / forget, and read (not written) by taskfw/context.py:lessons_for",
        "recall(query=...) matches by ORing the query's whitespace-split terms (not FTS5's default implicit AND) and orders results by bm25(memories_fts) relevance; the empty-query 'list recent' path is the only one still ordered by updated_at",
        "recall()'s default limit is DEFAULT_RECALL_LIMIT (5), defined in taskfw/config.py and env-overridable via TASKFW_DEFAULT_RECALL_LIMIT — not a literal in memory.py",
        "recall(count_hit=False) performs the same search with the counter write suppressed (task:60ebca8e). It exists for exactly one caller, context.lessons_for: bundle assembly is not a deliberate reach for a lesson, so counting it would leave hit_count measuring tasks__context calls with no way to separate the two afterwards. The default stays True, so every deliberate task_memory__recall still counts.",
        "memories_fts indexes 'slug text' and has NO tags column — relevant because context.lessons_for reuses related_candidates' 'title + tags' query string, where tags carry a 3:1 weighting on the tasks side that has no equivalent here"
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "Memory scoped to the loop: the lessons introspection produces that nothing else can hold — a constraint discovered the hard way, a technique worth reusing, a recurring pitfall. Graded risks, decisions, and architectural facts already have homes, so this deliberately covers only what is left over, and must not grow into a general knowledge store. As of task:60ebca8e it also has a guaranteed read path: the `lessons` section of tasks__context.",
      "evidence": [
        "taskfw/memory.py:MemoryStore.record",
        "taskfw/memory.py:MemoryStore.recall",
        "taskfw/memory.py:MemoryStore._matching_slugs",
        "taskfw/memory.py:MemoryStore._fts_or_query",
        "taskfw/memory.py:_standing",
        "taskfw/config.py:DEFAULT_RECALL_LIMIT",
        "taskfw/context.py:lessons_for",
        "taskfw/db/schema.py:MEMORIES",
        "tests/test_memory.py:TestStanding",
        "tests/test_memory.py:TestSupersede",
        "tests/test_memory.py:TestRecall",
        "tests/test_memory.py:TestUsageTracking",
        "tests/test_context.py:TestLessons"
      ],
      "invariants": [
        "Standing is DERIVED from the confirm/contradict links on every read and is never stored — a stored confidence would agree with whatever was typed",
        "A `disputed` memory is reported as disputed rather than resolved: the store cannot know which side is right, and returning it as settled would be an omission indistinguishable from an absence. This now matters more than it did: a disputed lesson arrives unasked-for inside every full tasks__context bundle, so the marking is what stops it reading as settled fact.",
        "Every memory cites the task it was learned from — an uncited lesson is an opinion and cannot be checked against the work that produced it",
        "Superseding keeps the row and names the replacement; forgetting is reserved for a lesson that was WRONG rather than outdated",
        "Superseded memories leave recall by default, because stale knowledge that keeps surfacing reads as current — and therefore never appear in a context bundle's lessons either, for free",
        "Links are idempotent, so re-running introspection cannot inflate a memory's standing",
        "Lives in the task database as one file rather than a second store; the memory-to-task citation is validated at the tool layer, NOT by a foreign key, so the single file is about having one source to check rather than about referential integrity. context.lessons_for depends on this: it opens a MemoryStore straight from store.conn when no instance is passed.",
        "Nothing is injected, but a memory now reaches an agent two ways: task_memory__recall, and the `lessons` section of a full tasks__context bundle (task:60ebca8e). Both are pulls — the agent called the tool — so the pull-not-push bargain is intact; what changed is that reading lessons back no longer depends on the agent independently remembering the recall tool exists. This supersedes the earlier invariant 'a memory reaches an agent only via task_memory__recall'.",
        "A malformed FTS5 query falls back to an unranked LIKE match rather than being treated as proof no memories exist",
        "hit_count/last_hit are purely observational usage signals, not staleness inputs — nothing currently reads them to drive supersede/forget automation (see task:582dba10, open). They count DELIBERATE recalls only: context bundle assembly passes count_hit=False precisely so the signal task:582dba10 would consume stays uncontaminated by automatic reads."
      ],
      "last_validated": "2026-08-23T04:34:37.417466+00:00",
      "module": "taskfw/memory.py",
      "name": "loop-memory-derived-standing"
    },
    "mcp-portable-interface": {
      "confidence": 0.93,
      "contracts": [
        "Every tool returns a dict; a refusal is {'error': reason, 'rule': which_rule}",
        "tasks__context with no argument falls back to the active task for the workspace",
        "tasks__finish is idempotent on an already-done task and refuses an abandoned one",
        "tasks__add_introspection appends where tasks__update(grooming=...) replaces",
        "tasks__format_commit_message(task_id, subject, body) validates the task exists and returns the canonical message shape",
        "tasks__logs(logger, level, limit) reads config.db_path() directly via log_conn(), never store().conn",
        "tasks__logs(logger, level, limit) filters by EXACT match only (SQL 'logger=?'/'level=?'), no LIKE/prefix/wildcard support -- auditing a family of related logger names (e.g. every 'taskfw.skill.<name>/<step>' row for one skill) requires either querying each exact name individually or pulling unfiltered rows and filtering client-side by string prefix (verified task:a0bb2c26).",
        "tasks__log_skill_invocation(skill, task_id='') is a thin wrapper around get_logger()",
        "tasks__phase(task_id) returns {id, groomed, implemented, introspected} or {'error': ...} for an unknown task",
        "tasks__create(...) returns {ok, id, type, status} plus an OPTIONAL related_candidates key (list of task summaries, context.related_candidates() against the new task's title) -- present only when at least one candidate is found, omitted entirely otherwise. Advisory only; never creates a tasks__link edge itself (task:78853657).",
        "_tool(hook=None) is the single decorator every tool carries instead of @mcp.tool(): it wraps fn in dispatcher.tool_called(fn.__name__, post=hook) and registers the WRAPPED function with mcp.tool() itself, so the module-level name bound is the logged version too. Plain tools use @_tool(); tools needing one or more nudges pass @_tool(hook=some_hook) or @_tool(hook=dispatcher.combine(hook_a, hook_b)).",
        "The 5 hook functions (_finish_reminder_hook, _ungroomed_progress_hook, _finish_hook, _stale_memory_hook, _introspection_hook) are all Callable[[dict], None], defined once near _tool and referenced by name at each tool's own decorator line -- never centrally dispatched by tool name. Hooks that need the post-mutation task re-fetch it via store().get(result['id']) rather than closing over a local variable computed mid-function, since the mutation is already persisted (store().save() already ran) by the time any hook fires -- this is what let hook-bearing and plain tools converge on one decorator instead of two different wiring mechanisms (task:58782207).",
        "tasks__set_active/tasks__clear_active best-effort POST {workspace, task_id, title} to claude-hooks' running server via _push_active_task (task:6906557f) -- a courtesy push, not a dependency; every failure mode (connection refused, timeout, DNS, non-2xx) is swallowed and logged at debug, and the tool's own return value/error semantics are unaffected either way.",
        "tasks__check_item(task_id, index, done=True) auto-activates task_id for the current scope as a direct side effect when done=True -- not via the _tool(hook=...) mechanism, but inline in the tool body via _auto_activate_on_checklist_progress. As of task:f5ace343 this is unconditional: active status is a single ephemeral pointer, not a stack, so checking an item on any task simply replaces whatever was active (no more lifecycle.check_active_switch confirm gate, no more 'active_task_notice' when a different task was active -- that gate and the check_active_switch function it called were both removed along with the stack). set_active is a no-op when task_id is already active. done=False (unchecking) never activates anything.",
        "tasks__set_active/tasks__active/tasks__clear_active response shapes carry no stack_depth or stack list (task:f5ace343) -- {'ok', 'active', 'scope'} / {'active', 'scope'} / {'ok', 'active': null, 'scope'} respectively, mirroring the single in-memory pointer store.set_active/get_active/clear_active now hold.",
        "tasks__create clears any active task for its scope on the way out (task:74bf3542) -- starting a new task is a clean break from whatever was active before it, not an implicit continuation."
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "The portable interface: MCP is the portability layer, so any MCP host reaches the whole framework through these tools with no neutral abstraction on top. Tools are in-process -- they import the library directly, with no daemon and no transport. Every mutating tool validates through lifecycle before calling store. As of task:58782207, every one of the ~35 registered tools is also wrapped in dispatcher.tool_called for unconditional logging via a single mechanism, mcp_server._tool -- the one route every tool goes through for registration + logging + an optional post-success hook. A tool skips registration entirely if it skips _tool; there is no 'registered but unlogged' state.",
      "evidence": [
        "taskfw/mcp_server.py:_tool",
        "taskfw/mcp_server.py:_finish_reminder_hook, _ungroomed_progress_hook, _finish_hook, _stale_memory_hook, _introspection_hook",
        "taskfw/mcp_server.py:_auto_activate_on_checklist_progress, tasks__check_item",
        "taskfw/mcp_server.py:tasks__create",
        "taskfw/mcp_server.py:tasks__finish",
        "taskfw/mcp_server.py:tasks__phase",
        "taskfw/mcp_server.py:_push_active_task, tasks__set_active, tasks__active, tasks__clear_active",
        "taskfw/drift_hook.py",
        "tests/test_mcp_tools.py::TestRegistration",
        "tests/test_mcp_tools.py::TestToolCallLogging",
        "tests/test_mcp_tools.py::TestPhase",
        "tests/test_mcp_tools.py::TestChecklist::test_checking_first_item_activates_the_task_when_nothing_else_is_active",
        "tests/test_mcp_tools.py::TestChecklist::test_checking_an_item_on_a_different_task_does_not_silently_switch",
        "tests/test_mcp_tools.py::TestCreate::test_surfaces_related_candidates_when_titles_overlap",
        "tests/test_mcp_tools.py::TestClaudeHooksPush"
      ],
      "invariants": [
        "No tool enforces a rule itself -- all validation goes through lifecycle.check_*, so a tool cannot enforce a different set than a hook",
        "The store is opened lazily, so importing the module never touches disk",
        "Active-task scope comes from TASKFW_SCOPE or cwd, never from a stored global",
        "No tool is missing the _tool wrapper and none is double-wrapped -- pinned by tests/test_mcp_tools.py::TestToolCallLogging, which enumerates the live m.mcp.list_tools() registry rather than a hand-maintained list, so a new tool that forgets the decorator fails this test rather than silently going unlogged",
        "related_candidates in tasks__create's response is computed inline in the tool body, NOT via the dispatcher hook/nudge mechanism -- every existing nudge is str | None, a candidate list is list[dict], a structurally different shape (task:78853657)",
        "_push_active_task reads CLAUDE_HOOKS_URL from os.environ on every call rather than caching it at import time, specifically so tests can redirect it via monkeypatch.setenv without needing to reload the module -- a module-level constant computed once at import would not observe a later monkeypatch.setenv (task:6906557f).",
        "There is no drift-reflection hook left in this module (task:8be768df removed _drift_reflection_hook entirely) -- drift-reflection awareness now lives solely in taskfw/drift_hook.py, a standalone PostToolUse hook Claude Code invokes directly on every tool call in the session, independent of any MCP tool call. tasks__check_item's auto-activation (see contracts) is unrelated to that removal -- it is a store mutation, not a nudge, and lives in mcp_server.py precisely because dispatcher.py's hooks never mutate stored state (see task_framework_system.sysml's TaskMCPTools doc).",
        "Active status is now exactly as long-lived as the pass that sets it (task:f5ace343) -- task-grooming and task-introspection skills activate/deactivate at their own pass boundaries rather than relying on any persistence layer."
      ],
      "last_validated": "2026-08-20T06:08:22.271963+00:00",
      "module": "taskfw/mcp_server.py",
      "name": "mcp-portable-interface"
    },
    "migration-writes-through-the-store": {
      "confidence": 0.8,
      "contracts": [],
      "created_at": "2026-08-01T02:51:02.588824+00:00",
      "description": "One-time import of claude-hooks' task store (~/.claude/proj_tasks.db) into taskfw. Every task is written through TaskStore.save() rather than by raw INSERT, because tasks.data holds the typed object while the scalar columns are projected from it, and save() is the sole writer of both. A bulk INSERT here would be a second writer whose rows disagree with themselves the moment either side changes. Side tables (events, edges, commits) are inserted directly since they carry no projected duplication. The type collapse is lossy by decision, not by accident: five claude-hooks issue types become taskfw's two, with the original preserved as a tag, because the extra four enforced no rule and were therefore already labels.",
      "evidence": [
        "taskfw/migrate_hooks.py",
        "taskfw/store.py",
        "taskfw/db/schema.py"
      ],
      "invariants": [
        "Tasks are written only through TaskStore.save(), never by direct INSERT into tasks",
        "Re-running changes nothing: save() upserts by id and side tables use INSERT OR IGNORE against their natural keys",
        "Parents are saved before children; a row whose parent is missing or cyclic is reported as unplaceable, never silently reparented",
        "Every source row is accounted for in the Report — migrated, empty, or orphaned — so a dropped row is never indistinguishable from one that never existed",
        "No status outside STATUSES is ever written; an unrecognised one becomes open and is tagged with what it was"
      ],
      "last_validated": "2026-08-01T02:51:02.589879+00:00",
      "module": "taskfw/migrate_hooks.py",
      "name": "migration-writes-through-the-store"
    },
    "pull-context-bundle": {
      "confidence": 0.95,
      "contracts": [
        "build_context(store, task_id, verbosity, memory=None) -> bundle, or {'error': ...} for an unknown task. `memory` is an optional MemoryStore; when omitted, build_context opens one from store.conn, since memories share the task database. mcp_server.tasks__context passes the shared singleton on purpose — MemoryStore.__init__ runs CREATE VIRTUAL TABLE IF NOT EXISTS and commits, so constructing one per call would put a write on a pure read path. NOTE the parameter is optional rather than required for call-site compatibility (task:60ebca8e decision), so the write-on-read failure mode is mitigated at the one production call site, not deleted; a second production caller should make it required.",
        "Sections in fixed order: task, decisions, grooming, graph, commits, related, lessons",
        "verbosity='summary' returns identity, status, progress, and open items only — it carries no lessons section, and returns before any MemoryStore is opened",
        "graph.edges is capped at MAX_EDGES (5) per direction (outgoing/incoming independently), sliced in _graph(); when either direction is capped, the drop counts are reported in a bundle-level `edges_truncated` dict, e.g. {'outgoing': 3, 'incoming': 2}",
        "related_candidates(store, task) (public, renamed from _related in task:78853657) passes task.title straight through as a plain string to store.search() -- search() itself builds the OR-of-terms FTS5 query AND ranks by _combination_score (tag overlap weighted 3x over body overlap), not bm25 `rank` as of task:f24a88e3. context.py no longer builds any FTS query string itself -- _title_or_query was removed once search() took over that responsibility, so context.py has no FTS-syntax-construction code left at all.",
        "lessons_for(store, task, memory=None) (public, task:60ebca8e) queries MemoryStore.recall with the same 'title + tags' string related_candidates uses, capped at MAX_LESSONS (3), and returns recall's rows unmodified so each lesson carries the `standing` derived in the memory store. It calls recall(count_hit=False): assembling a bundle must not bump hit_count/last_hit, which exist to measure DELIBERATE recall.",
        "related_candidates has NO minimum-relevance floor — it always fills up to MAX_RELATED with the best-available matches among ALL tasks in the store, however weak, since ranking-by-relevance only orders candidates, it doesn't exclude poor ones. In a store spanning multiple unrelated projects this can surface genuinely unrelated tasks sharing only generic words (observed live, task:78853657, and again on task:60ebca8e, where a task about loop memory in the context bundle drew 'Add claude-hooks to cwd_domain_map' and 'Remove LLM from certificate renewal flow').",
        "lessons_for has no relevance floor either, for the same structural reason. MAX_LESSONS was set below MAX_RELATED as the intended brake — a weak related task reads as an ignorable pointer, whereas a weak lesson reads as advice, and inapplicable advice is worse than none. Task:60ebca8e's own introspection graded that brake INEFFECTIVE: live, the section filled all three slots with memories about CLI-tool integration, slug invention and concept-store scoping, none bearing on the task. A cap bounds output count; only a floor bounds output quality. Treat MAX_LESSONS as a volume limit, not a quality mechanism."
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "build_context assembles the whole working bundle for a task in one call — the sole replacement for per-turn prompt injection. Fixed section order, a documented trim order dropping whole sections least-useful-first, and a truncated list so an omitted section is distinguishable from an empty one. Independently, several sections also carry a fixed per-section cap (MAX_DECISIONS, MAX_COMMITS, MAX_RELATED, MAX_LESSONS, MAX_EDGES) applied by slicing before CHAR_BUDGET enforcement ever runs — a separate, silent mechanism from the whole-section drop that TRIM_ORDER implements, except for edges: edges' cap is the one case that IS surfaced, via `edges_truncated`. As of task:60ebca8e the bundle has a seventh section, `lessons`, which is the guaranteed read path for loop memory.",
      "evidence": [
        "taskfw/context.py:build_context",
        "taskfw/context.py:_graph",
        "taskfw/context.py:related_candidates",
        "taskfw/context.py:lessons_for",
        "taskfw/context.py:_enforce_budget",
        "taskfw/context.py:_trim_grooming",
        "taskfw/context.py:TRIM_ORDER",
        "taskfw/context.py:GROOMING_TRIM_ORDER",
        "taskfw/context.py:MAX_EDGES",
        "taskfw/context.py:MAX_LESSONS",
        "taskfw/memory.py:MemoryStore.recall",
        "taskfw/store.py:TaskStore.search",
        "taskfw/store.py:TaskStore._combination_score",
        "taskfw/mcp_server.py:tasks__create",
        "taskfw/mcp_server.py:tasks__context",
        "tests/test_context.py:TestBudget",
        "tests/test_context.py:TestEdgesCap",
        "tests/test_context.py:TestLessons",
        "tests/test_context.py:TestGroomingTrim",
        "tests/test_context.py::test_related_matches_on_partial_word_overlap"
      ],
      "invariants": [
        "The task section is NEVER trimmed — a bundle without its task is useless rather than merely large",
        "CHAR_BUDGET-driven trimming (_enforce_budget) drops whole sections, never parts of one, for every section EXCEPT grooming: a half-listed set of commits/related/lessons/graph/decisions reads as complete and is more misleading than an absent section that says so. Anything dropped whole is named in `truncated`.",
        "grooming is the one section trimmed field-by-field instead of whole (task:1c31a060, fixing a measured cost on task:60ebca8e: grooming was 8371 chars against task=4087/decisions=1890, so whole-section dropping evicted it entirely and landed the bundle at 6155 chars with 5845 unspent — the more thoroughly a task was groomed, the more certainly its grooming vanished from the main entry point, breaking introspection's ability to grade its own predictions). _trim_grooming now drops grooming's own fields least-useful-first per GROOMING_TRIM_ORDER (suggested_improvements, prior_art, hidden_assumptions, risks, open_questions, clarifications — clarifications last, being verified facts, the highest-value content grooming produces) until the bundle fits or grooming has nothing left. Fields actually dropped are named in `grooming_truncated`, a field distinct from `truncated` for the same reason `edges_truncated` is: a caller reading only the response can tell partial grooming from complete grooming, and knows not to round-trip a partial dict through tasks__update's wholesale-replace. Only if grooming trims down to {} does it fall through to a normal whole-section drop and get named in `truncated` like every other section.",
        "Separately, MAX_DECISIONS/MAX_COMMITS/MAX_RELATED/MAX_LESSONS apply a fixed pre-slice cap regardless of CHAR_BUDGET, and do so SILENTLY — none of them appear in `truncated` when the fixed cap (not the budget) is what cut the list. This is a known asymmetry, not yet resolved for the other four.",
        "MAX_EDGES is the one fixed cap on a non-grooming section that reports its own truncation, via `edges_truncated` — a field distinct from `truncated` on purpose: `truncated` means CHAR_BUDGET dropped a whole section, `edges_truncated` means the section is present but its edges list was capped",
        "If CHAR_BUDGET forces `graph` to drop entirely, `edges_truncated` is cleared too, so a truncation count can never outlive the section it describes",
        "related and lessons are the TWO approximate sections and the first two trimmed, in that order (TRIM_ORDER = related, lessons, commits, graph, grooming, decisions). Every other section is an exact lookup — grooming included, despite now being trimmed like an approximate one; its position in TRIM_ORDER and its exactness are unrelated to whether it can be partially rendered.",
        "The 'title + tags' query is a description of what a task IS about, not of what it needs to KNOW, and fails in both directions (task:60ebca8e). Besides admitting weak matches, it EXCLUDES strong ones whose vocabulary the title and tags happen not to contain.",
        "lessons is always a list, never a dict — so that _enforce_budget's isinstance branch resets it to [] and an omitted lessons section stays distinguishable from a task that simply has no matching memories. grooming is the one section that is a dict, which is exactly why it needed its own trim path (_trim_grooming) rather than reusing the list-based isinstance branch.",
        "Building a bundle has NO write side effect on loop memory: hit_count and last_hit are untouched by tasks__context, so they keep measuring deliberate task_memory__recall only (pinned by tests/test_context.py::TestLessons::test_assembling_a_bundle_does_not_bump_hit_count)"
      ],
      "last_validated": "2026-08-23T05:17:43.351210+00:00",
      "module": "taskfw/context.py",
      "name": "pull-context-bundle"
    },
    "schema-single-source-additive-migration": {
      "confidence": 0.95,
      "contracts": [
        "migrate(conn) -> {'created': [...], 'added': ['table.column'], 'orphaned': ['table.column']}",
        "Table.create_sql() generates DDL from the same columns migration diffs against"
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "TABLES is one declarative structure of Table/Column dataclasses that generates DDL, drives migration, and is read directly by tests. migrate() creates missing tables and adds missing columns, reporting created/added/orphaned. Additive-only is enforced by construction rather than policy: no code path emits DROP or a rewriting ALTER.",
      "evidence": [
        "taskfw/db/schema.py:TABLES",
        "taskfw/db/schema.py:migrate",
        "tests/test_foundation.py:TestAdditiveOnly"
      ],
      "invariants": [
        "Only CREATE TABLE IF NOT EXISTS and ALTER TABLE ADD COLUMN are ever emitted",
        "A column present in the DB but absent from TABLES is reported as orphaned and never dropped",
        "There is exactly one DDL definition — tests read TABLES rather than restating it, so no schema-parity check is needed",
        "ALTER TABLE ADD COLUMN cannot take a non-constant DEFAULT, so _addable_decl strips it; a migrated column is therefore not byte-identical to the same column created fresh"
      ],
      "last_validated": "2026-08-01T00:00:00+00:00",
      "module": "taskfw/db/schema.py",
      "name": "schema-single-source-additive-migration"
    },
    "stderr-only-logging": {
      "confidence": 0.92,
      "contracts": [
        "get_logger(name) -> logger under the 'taskfw' root, configured on first call",
        "Level comes from TASKFW_LOG_LEVEL, default INFO",
        "SQLiteHandler and JsonlHandler both swallow every write exception — a broken log write must never break the tool call that triggered it",
        "SQLiteHandler caches one sqlite3 connection per thread (threading.local()), not one per process — a single process-wide connection eventually gets used from a thread that didn't create it (an MCP server dispatches sync tool calls across a worker thread pool), and sqlite3 raises ProgrammingError on that, which the bare except silently swallows for the rest of the process's life (task:7fff42f4)"
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "Stderr is always one of the sinks, configured once. An MCP server speaks JSON-RPC over stdout, so a stray write there corrupts the protocol — that is a correctness constraint for the whole project, not a formatting preference, and it bans stdout specifically, not persistence generally. A second sink also exists: SQLiteHandler persists into the `logs` table (the operational-trace half of observability, alongside task_events' durable record of decisions and outcomes) in normal operation, or JsonlHandler when TASKFW_LOG_JSONL is set (test runs only, via tests/conftest.py) — a test run must never add a row to `logs`, isolated database or not, so it writes one JSON-per-line file instead, truncated fresh per run.",
      "evidence": [
        "taskfw/log.py:get_logger",
        "taskfw/log.py:SQLiteHandler",
        "taskfw/log.py:JsonlHandler",
        "tests/test_logging.py:TestLogDestination",
        "tests/test_logging.py:TestSQLiteSink",
        "tests/test_logging.py:TestSQLiteSink::test_emit_from_a_second_thread_still_lands",
        "tests/test_logging.py:TestJsonlSink"
      ],
      "invariants": [
        "No handler under the taskfw logger may write to stdout — pinned by tests/test_logging.py",
        "The root taskfw logger does not propagate, so host logging config cannot redirect it to stdout",
        "Denials log at INFO and allows at DEBUG, so a full trace is one env var away and costs nothing normally",
        "SQLiteHandler always targets config.db_path(), never whatever TaskStore is currently active — log.py's handlers are process-wide singletons with no store to draw a per-call path from",
        "A SQLiteHandler instance never uses the same sqlite3 connection object across two threads — each thread that calls emit() gets its own connection via threading.local(), so cross-thread reuse (the cause of a total, silent, permanent log-sink death) is unrepresentable rather than merely caught"
      ],
      "last_validated": "2026-08-04T11:49:46.303497+00:00",
      "module": "taskfw/log.py",
      "name": "stderr-only-logging"
    },
    "sysml-doc-only-model-enforcement": {
      "confidence": 0.8,
      "contracts": [
        "A port def enumerating an RPC/tool surface (e.g. mcp_interface.sysml's TasksPort/ConceptPort/TaskMemoryPort) is checked by enumerating the LIVE registry at test time (e.g. asyncio.run(m.mcp.list_tools())) rather than importing a static name list, since the registry itself is the source of truth for what is currently registered, not a constant a human could also hand-copy."
      ],
      "created_at": "2026-08-17T13:45:53.662411+00:00",
      "description": "SysML v2 structural models in this repo (state defs, calc defs, interface defs/port defs) are transcribed by hand from Python source rather than parsed/bound to it. They are kept honest not by execution but by a paired pytest class that imports the real Python source of truth and string-matches its identifiers/literals against the .sysml file text.",
      "evidence": [
        "models/mcp_interface.sysml",
        "tests/test_models.py:TestMcpInterfaceMatchesCode"
      ],
      "invariants": [
        "A calc def or state def with no matching TestXMatchesCode test is an unenforced restatement of the same claim the prose already made — not acceptable per this task's motivation.",
        "Test risk text and grading must stay byte-identical to grooming's `risks` entries for tasks__grooming_accuracy recurrence tracking to work."
      ],
      "last_validated": "2026-08-17T14:02:54.919350+00:00",
      "module": "models/task_lifecycle.sysml",
      "name": "sysml-doc-only-model-enforcement"
    },
    "tasks-table-projection-contract": {
      "confidence": 0.85,
      "contracts": [
        "schema.TASKS declares the 5 scalar columns (id, type, status, parent, title) plus data/created_at/updated_at — adding a 6th scalar means updating BOTH schema.TASKS and the explicit column list in TaskStore.save()'s INSERT/ON CONFLICT statement, which is not derived from TABLES",
        "Task (models.py) is the superset: every schema scalar name (type, status, parent, title, id) must exist as a same-named Task field, or save() breaks on task.<attr> access",
        "TaskStore.save(task) is the ONLY place that writes tasks.type/status/parent/title — no other method in store.py, lifecycle.py, or mcp_server.py issues an UPDATE/INSERT against those columns",
        "TaskStore.list(status=, type=, parent=) and TaskStore.children() filter on the scalar columns only — a field promoted into `data` but not mirrored as a scalar is invisible to these filters and to idx_tasks_status/idx_tasks_parent",
        "TaskStore.get(task_id) and .search() read ONLY the `data` column and reconstruct via Task.from_json — they never read the scalars, so a scalar/data mismatch is invisible to these two paths specifically"
      ],
      "created_at": "2026-08-08T04:30:33.942098+00:00",
      "description": "The tasks table has two kinds of columns that must stay in lockstep: five indexable scalars (id, type, status, parent, title) that mirror fields on Task, and one `data` column holding the full Task as JSON, which is authoritative for every field not promoted to a scalar. There is exactly one writer of both — TaskStore.save() — which is what prevents the projection from drifting. Anything that reads a scalar (tasks__list's status/type/parent filters, the idx_tasks_status/idx_tasks_parent indexes) is reading a value that also exists inside `data`; anything that reads `data` (Task.from_json, every other field) never touches the scalars at all.",
      "evidence": [
        "taskfw/db/schema.py:TASKS",
        "taskfw/models.py:Task",
        "taskfw/store.py:TaskStore.save",
        "taskfw/store.py:TaskStore.list",
        "taskfw/store.py:TaskStore.children",
        "taskfw/mcp_server.py:tasks__list"
      ],
      "invariants": [
        "A scalar column name in schema.TASKS that has no matching Task field is a silent breakage point: save() would raise AttributeError on task.<name>, but nothing enforces the two lists match except manual read-through — there is no test pinning column-name/field-name parity the way test_foundation.py pins additive-only migration",
        "Renaming a Task field without renaming the corresponding schema.TASKS column (or vice versa) is exactly the kind of change concept__search on the old/new name should catch before it ships",
        "New Task fields do NOT need a matching scalar column by default — only fields that need to be filtered/indexed independent of JSON parsing get promoted (see schema.py's own comment on TASKS.data)",
        "schema.migrate()'s additive-only guarantee (see schema-single-source-additive-migration) means a scalar column can be added but never renamed in place — a Task field rename therefore always means a NEW scalar column plus an orphaned old one, not an in-place rename"
      ],
      "last_validated": "2026-08-08T05:41:07.507641+00:00",
      "module": "taskfw/store.py",
      "name": "tasks-table-projection-contract"
    },
    "taskstore-data-access-only": {
      "confidence": 0.94,
      "contracts": [
        "save(task) upserts and returns the task; get(id) -> Task | None",
        "link() and add_commit() return True when a row was created, False when it already existed",
        "unlink(from_id, to_id, rel=None) -> rows removed; omitting rel removes every edge between the pair",
        "search(query, limit) falls back to LIKE when FTS5 is unavailable or the query is malformed",
        "search(query)'s `query` is a PLAIN string, not pre-formatted FTS5 syntax -- search() builds its own OR-of-terms MATCH expression internally (each term quoted, so punctuation is read as literal text). Callers (tasks__search, context.related_candidates()) never construct FTS syntax themselves.",
        "search() ranks results by _combination_score(task, terms), NOT FTS5's own bm25 `rank` -- tag-word overlap is weighted 3x over body-word overlap (_TAG_WEIGHT/_BODY_WEIGHT), mirroring claude-hooks' LoadMemoriesNode combination scorer (task:c3b53021). FTS5 MATCH still does candidate recall (over-fetched at limit*4); only the final ordering is Python-side (task:f24a88e3).",
        "set_active(task_id, scope)/get_active(scope)/clear_active(scope) replaced push_active/pop_active/active_stack (task:f5ace343) -- a plain in-memory dict (scope -> task_id) on TaskStore, gone the moment the process ends. set_active just overwrites scope's entry; no push/no-op-on-already-top special case. clear_active pops the scope key and logs only if something was actually cleared."
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "Persistence for tasks, events, edges, commits, and the active-task pointer. Contains no rule enforcement at all, so LifecycleRules keeps a single allocation; a store method rejects only what it structurally cannot do. save() is the sole writer of both the scalar columns and the JSON blob, which is what keeps them from drifting.",
      "evidence": [
        "taskfw/store.py:TaskStore.save",
        "taskfw/store.py:TaskStore.unlink",
        "taskfw/store.py:TaskStore.search",
        "taskfw/store.py:TaskStore._combination_score",
        "taskfw/store.py:TaskStore.set_active",
        "taskfw/store.py:TaskStore.get_active",
        "taskfw/store.py:TaskStore.clear_active",
        "tests/test_store.py"
      ],
      "invariants": [
        "No store method ever rejects a write for a policy reason -- that is LifecycleRules' job",
        "save() is the only writer of the scalar columns and `data`, which is what prevents projection drift",
        "Edge and commit inserts are idempotent via table constraints, not application checks",
        "The active-task state is a single in-memory pointer per scope, NOT persisted and does NOT survive a restart (task:f5ace343) -- replaced the durable per-scope LIFO stack (ACTIVE_TASK_STACK, itself a replacement for the older single-row ACTIVE_TASK table, task:f302eb2b). ACTIVE_TASK_STACK is dropped from schema.TABLES but its Table()/index constant stays defined per the additive-only migration rule, so pre-existing rows are left in place, orphaned.",
        "FTS5 absence degrades search rather than failing at import",
        "_combination_score scores tags and body against DISJOINT word sets -- a word counted as a tag match is excluded from the body-overlap count, so it can never score twice"
      ],
      "last_validated": "2026-08-20T06:06:54.058538+00:00",
      "module": "taskfw/store.py",
      "name": "taskstore-data-access-only"
    },
    "typed-task-object": {
      "confidence": 0.95,
      "contracts": [
        "Task.to_dict()/to_json() and Task.from_dict()/from_json() round-trip every field",
        "Task.progress -> (done, total), derived from resolution on every read",
        "Task.search_text() flattens the fields worth indexing",
        "TASK_EDGE_RELATIONS is the closed vocabulary for task_edges.rel (10 relations: depends_on, blocks, relates_to, duplicates, removes, implements, supersedes, reverts, writes_to, extends), enforced by lifecycle.check_link_rel on tasks__link. Confirmed against real usage in task_edges before being formalised (task:1c9d37d7) — 4 trace to Jira's issue-link-type vocabulary, the other 6 are this framework's own emergent vocabulary. parent_of was deliberately excluded as redundant with Task.parent."
      ],
      "created_at": "2026-08-01T00:00:00+00:00",
      "description": "A task is one typed dataclass serialised to JSON. Two issue types only (epic, task) and four statuses. Typing removes two things outright rather than relocating them: required-section validation becomes schema validation, and 'resolution must be a checklist' becomes unrepresentable because resolution IS a list of items.",
      "evidence": [
        "taskfw/models.py:Task",
        "taskfw/models.py:Task.progress",
        "taskfw/models.py:TASK_EDGE_RELATIONS",
        "tests/test_store.py:TestRoundTrip",
        "tests/test_lifecycle.py:TestCheckLinkRel"
      ],
      "invariants": [
        "from_dict drops unknown keys rather than raising, so a store written by a newer version stays readable by an older one",
        "progress is never stored — it cannot drift from the list it counts",
        "notes exists deliberately as the escape hatch for anything the schema did not anticipate, and must survive later tidying",
        "Links are never stored in the task blob; they live in task_edges where the primary key makes them idempotent",
        "TASK_EDGE_RELATIONS is enforced only on creation (tasks__link) — removal (tasks__unlink) is deliberately ungated so a legacy or invalid rel value never becomes permanently stuck"
      ],
      "last_validated": "2026-08-06T00:49:28.638637+00:00",
      "module": "taskfw/models.py",
      "name": "typed-task-object"
    }
  },
  "meta": {
    "note": "Architectural concepts, one or more per module. Ground truth is the code; this is a durable summary of what each module promises and what must stay true of it. The SysML model under models/ is derived FROM this, never the reverse — code -> concepts -> model, one direction only.",
    "repo": "task-framework",
    "seeded_at": "2026-08-01",
    "updated_at": "2026-08-23T05:17:43.351356+00:00"
  }
}
